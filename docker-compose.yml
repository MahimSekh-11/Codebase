version: '3.8'

services:
  codebase-rag:
    build: .
    container_name: codebase-rag-app
    ports:
      - "8000:8000"
      - "8501:8501"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
